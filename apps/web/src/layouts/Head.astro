---
import { ClientRouter } from 'astro:transitions';

interface Props {
    title?: string;
}

const { title = 'ui craft' } = Astro.props;
---

<title>{title}</title>
<meta name='title' content={title} />
<ClientRouter />
